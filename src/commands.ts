// Ribbon commands function file. The ribbon button uses a ShowTaskpane action,
// so there are no ExecuteFunction handlers to register — this page just needs
// to signal Office that it's ready.
Office.onReady(() => {
  // Nothing to do here; the task pane does all the work.
});
